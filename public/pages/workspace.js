import {escape,icon,button,drawer,metric,badge,empty,tabs,enhance,table} from '../components/ui/index.js';
import {navigate} from '../components/layout/app-shell.js';
import {memoryKinds,renderMemoryList} from '../memory.js';
import {fmtNum,fmtDate,renderDonut,renderSparkline,renderTrendChart,installTrendChart,chartColors,stageNames} from '../format.js';
const $=s=>document.querySelector(s),formDrawers=new Map();let cache=new Map(),user,openLead=false,apiClient,actionLabels={};
function action(route,text,fn){const b=button(text,{variant:'primary',iconName:'plus'});$(`[data-page="${route}"] .page-actions`).append(b);b.onclick=fn;return b;}
export function installWorkspace(){
 for(const [id,route,title] of [['draft','content','مسودة جديدة'],['ai-form','content','توليد محتوى بالذكاء الاصطناعي'],['memory-form','knowledge','إضافة معلومة'],['user-form','users','إضافة عضو'],['calendar-form','planning','إنشاء خطة محتوى'],['schedule-form','planning','جدولة محتوى'],['crm-create','crm','إضافة عميل']]){
  const form=$('#'+id),dialog=drawer(title,form);dialog.close();const trigger=action(route,title,()=>dialog.showModal());if(['ai-form','schedule-form'].includes(id))trigger.className='button secondary';formDrawers.set(id,{dialog,trigger});
 }
 const detail=$('#crm-detail'),dialog=drawer('ملف العميل',detail);dialog.close();formDrawers.set('crm-detail',{dialog});
 const plan=$('#planning'),planningLayout=document.createElement('div');planningLayout.className='planning-layout';const approvalPanel=$('#brief-view').closest('.panel'),calendar=$('#calendar-view');plan.querySelector('.workspace').before(planningLayout);planningLayout.append(approvalPanel,calendar);
 const contentPage=$('[data-page=content]'),generation=$('#generation'),content=$('#content');contentPage.insertBefore($('#stats'),generation);
 const approvals=document.createElement('section');approvals.id='content-approval-view';const publishing=document.createElement('section');publishing.id='content-publishing-view';contentPage.append(approvals,publishing);
 contentPage.insertBefore(content,generation);const tabHost=document.createElement('div');contentPage.insertBefore(tabHost,content);tabs(tabHost,[['المحتوى',content],['الموافقات',approvals],['التوليد بالذكاء الاصطناعي',generation],['النشر والجدولة',publishing]]);
 const contentWorkspace=content.querySelector('.workspace');contentWorkspace.className='content-layout';const rail=document.createElement('div');rail.className='content-rail';rail.innerHTML=`<span class="metric-icon">${icon('agent')}</span><h3>مساعد الكتابة</h3><p>ابدأ بفكرة ومنتج معتمد لإنشاء مسودة عربية وإنجليزية.</p>`;const generate=button('إنشاء مسودة بالذكاء الاصطناعي');generate.onclick=()=>formDrawers.get('ai-form').dialog.showModal();rail.append(generate);contentWorkspace.append(rail);rail.id='content-ai-rail';
 const toolbar=document.createElement('div');toolbar.className='collection-toolbar';toolbar.innerHTML='<input id="content-search" type="search" aria-label="بحث المحتوى" placeholder="ابحث في المحتوى…"><select id="content-status" aria-label="حالة المحتوى"><option value="">كل الحالات</option><option value="DRAFT">مسودة</option><option value="REVIEWED">بانتظار الاعتماد</option><option value="APPROVED">معتمد</option><option value="REJECTED">مرفوض</option><option value="SUPERSEDED">إصدار سابق</option></select><select id="content-platform" aria-label="منصة المحتوى"><option value="">كل المنصات</option><option>Instagram</option><option>X</option><option>Facebook</option><option>LinkedIn</option></select>';contentWorkspace.before(toolbar);toolbar.oninput=filterContent;
 const memoryWorkspace=$('#knowledge .workspace');memoryWorkspace.className='memory-layout';const categories=document.createElement('div');categories.className='memory-categories';categories.setAttribute('aria-label','تصنيفات المعرفة');for(const [key,label] of [['','كل المعلومات'],...Object.entries(memoryKinds)]){const b=button(label,{variant:'ghost'});b.dataset.kind=key;b.onclick=()=>{categories.querySelectorAll('button').forEach(el=>el.classList.toggle('active',el===b));$('#memory-list').dataset.kind=key;renderMemoryList();};categories.append(b);}categories.firstElementChild.classList.add('active');memoryWorkspace.prepend(categories);
 const search=document.createElement('input');search.type='search';search.id='memory-search';search.placeholder='ابحث في ذاكرة العلامة… (المفتاح، القيمة، المنتج، المصدر، النوع)';search.setAttribute('aria-label','بحث الذاكرة');search.oninput=renderMemoryList;memoryWorkspace.before(search);
 action('knowledge','مراجعة التغييرات',()=>$('#memory-approvals').scrollIntoView({behavior:'smooth',block:'start'})).className='button secondary';
 action('integrations','فحص حالة جميع التكاملات',()=>{}).id='integrations-check-all';
 document.addEventListener('memory-form-open',()=>formDrawers.get('memory-form').dialog.showModal());
 const fab=button('',{variant:'ghost',iconName:'agent','aria-label':'فتح غرفة تحكم Frost'});fab.className='fab-shortcut';fab.onclick=()=>navigate('agents');document.body.append(fab);
 document.addEventListener('click',e=>{const lead=e.target.closest('[data-lead-id],[data-open-lead]');if(lead)openLead=true;const quick=e.target.closest('[data-quick-action]');if(quick&&['add-lead','add-b2b'].includes(quick.dataset.quickAction)){const form=$('#crm-create');form.elements.customerType.value=quick.dataset.quickAction==='add-b2b'?'B2B':'B2C';formDrawers.get('crm-create').dialog.showModal();}});
 document.addEventListener('click',e=>{const b=e.target.closest('[data-content-open]');if(!b)return;openContentItem(b.dataset.contentOpen);});
}
export function beforeWorkspaceRender(){for(const dialog of document.querySelectorAll('dialog[open]'))dialog.close();}
export function workspaceAuth(auth){const role=auth.user?.role;for(const [id,item] of formDrawers){if(!item.trigger)continue;item.trigger.hidden=!role||(['user-form','schedule-form'].includes(id)?role!=='owner':role==='reviewer');}if(!role){cache=new Map();user=null;$('#audit-list').replaceChildren();}}
export function renderWorkspace(data,auth,api,labels={}){cache=data;user=auth.user;apiClient=api;actionLabels=labels;
 for(const [id,item] of formDrawers)if(item.trigger)item.trigger.hidden=$('#'+id).hidden;
 $('#content-ai-rail').hidden=user.role==='reviewer';
 renderOverview();renderCollections();renderCalendar();
 $('#agent-metrics')?.remove();const agents=cache.get('/api/agents')||[],metrics=document.createElement('div');metrics.id='agent-metrics';metrics.className='kpi-grid';metrics.innerHTML=metric('الوكلاء',agents.length,'الأدوار المسجلة','agent')+metric('متاح للتشغيل',agents.filter(a=>a.runtimeStatus==='ONLINE').length,'وفق حالة التشغيل الحالية')+metric('ينتظر التكامل',agents.filter(a=>['WAITING_INTEGRATION','WAITING_LLM'].includes(a.runtimeStatus)).length,'إعداد خدمة مطلوب','plug')+metric('قرارات معلقة',(cache.get('/api/approvals?status=PENDING')||[]).length,'تحتاج مراجعة بشرية','file');$('#frost-control').before(metrics);
 $('#agent-list').querySelectorAll(':scope>.card').forEach((card,index)=>{const b=button('التفاصيل والتشغيل',{variant:'secondary',iconName:'arrow'});b.classList.add('agent-open');b.onclick=async()=>{const d=drawer(agents[index]?.name||'تفاصيل الوكيل',card,{restore:true});const history=document.createElement('section');history.className='agent-history';history.innerHTML='<h3>آخر التشغيلات</h3><p role="status">جارٍ تحميل السجل…</p>';d.querySelector('.dialog-body').append(history);try{const runs=await apiClient('/api/agents/'+agents[index].id+'/runs');history.innerHTML='<h3>آخر التشغيلات</h3>'+(runs.length?table(['الحالة','المحفز','وقت البدء'],runs.map(r=>[badge(r.status),escape(r.trigger_type),escape(r.started_at||'—')])):empty('لا تشغيلات مسجلة لهذا الوكيل'));enhance(history);}catch(error){history.textContent='تعذر تحميل السجل: '+error.message;}};card.append(b);});
 $('#items').querySelectorAll(':scope>article').forEach((card,index)=>{card.dataset.itemId=cache.get('/api/state').content[index].id;const b=button('فتح المحتوى',{variant:'secondary',iconName:'arrow'});b.classList.add('content-open');b.onclick=()=>drawer('تفاصيل المحتوى والمراجعة',card,{restore:true});card.append(b);});filterContent();
 if(openLead){formDrawers.get('crm-detail').dialog.showModal();openLead=false;}
 document.querySelectorAll('.kanban').forEach(el=>{el.tabIndex=0;el.setAttribute('role','region');el.setAttribute('aria-label','لوحة مراحل المبيعات');el.querySelectorAll('[data-lead-drag]').forEach(card=>{const b=button('فتح الملف',{variant:'ghost','data-open-lead':card.dataset.leadDrag});card.append(b);});});
 enhance();
}
// Real per-week series from saved executive reports (buildExecutiveReport extras), deduped
// by weekStart and always ending with the current, still-unsaved week — never fabricated
// beyond however many weeks the owner has actually saved.
function weeklySeries(reportsData,pick) {
 const byWeek=new Map();
 for(const entry of reportsData?.saved||[])if(entry.kpis)byWeek.set(entry.weekStart,entry);
 if(reportsData?.current?.kpis)byWeek.set(reportsData.current.weekStart,reportsData.current);
 return [...byWeek.values()].sort((a,b)=>a.weekStart.localeCompare(b.weekStart)).slice(-8).map(entry=>({label:fmtDate(entry.weekStart),value:pick(entry.kpis)}));
}
function renderOverview(){const state=cache.get('/api/state'),agents=cache.get('/api/agents')||[],plan=cache.get('/api/planning'),frost=cache.get('/api/frost/status'),crm=cache.get('/api/crm'),approvals=cache.get('/api/approvals?status=PENDING')||[],reportsData=cache.get('/api/reports/weekly'),pending=state.content.filter(c=>['DRAFT','REVIEWED'].includes(c.status));let host=$('#overview-dashboard');if(!host){host=document.createElement('div');host.id='overview-dashboard';$('[data-page=overview]').append(host);}
 const currentKpis=reportsData?.current?.kpis;
 const revenueSeries=weeklySeries(reportsData,k=>k.wonRevenue.value);
 const leadsSeries=weeklySeries(reportsData,k=>k.leadsCreated.value);
 const wonSeries=weeklySeries(reportsData,k=>k.wonDeals.value);
 const statusLabels={ONLINE:'جاهز للعمل',WAITING_INTEGRATION:'بانتظار تكامل',WAITING_LLM:'بانتظار مزود الذكاء الاصطناعي',DISABLED:'معطّل'};
 const statusIcons={ONLINE:'agent',WAITING_INTEGRATION:'plug',WAITING_LLM:'plug',DISABLED:'close'};
 const statusColors={ONLINE:'chart-c3',WAITING_INTEGRATION:'chart-c4',WAITING_LLM:'chart-c4',DISABLED:'chart-c6'};
 const byStatus=['ONLINE','WAITING_INTEGRATION','WAITING_LLM','DISABLED'].map(status=>({label:statusLabels[status],value:agents.filter(a=>a.runtimeStatus===status).length,status}));
 const stageCounts={};for(const lead of crm?.leads||[])stageCounts[lead.stage]=(stageCounts[lead.stage]||0)+1;
 const stageSegments=Object.entries(stageCounts).map(([stage,value])=>({label:stageNames[stage]||stage,value}));
 host.innerHTML=`
<div class="hero-row">
 <div class="hero-card"><div class="hero-card-top"><span class="hero-icon">${icon('agent')}</span><div><h2>مرحبًا بعودتك، ${escape(user.name.split(' ')[0])}</h2><p>لقطة مباشرة من مساحة العمل الآن</p></div></div><div class="hero-stats"><div><span>محتوى ينتظر القرار</span><strong dir="ltr">${fmtNum(pending.length)}</strong></div><div><span>قرارات الوكلاء المعلقة</span><strong dir="ltr">${fmtNum(approvals.length)}</strong></div><div><span>فرص العملاء</span><strong dir="ltr">${fmtNum(crm?.leads?.length)}</strong></div></div></div>
 <div class="trend-card"><div class="row-between"><h3>الإيرادات المكتسبة أسبوعيًا</h3></div>${renderTrendChart(revenueSeries,escape)}</div>
</div>
<div class="mini-row">
 <div class="mini-stat-card tone-a"><div class="mini-stat-head"><span class="mini-stat-icon">${icon('users')}</span><span class="kpi-label">فرص جديدة هذا الأسبوع</span></div><strong class="kpi-value" dir="ltr">${fmtNum(currentKpis?.leadsCreated?.value)}</strong>${renderSparkline(leadsSeries.map(p=>p.value),'chart-c2')}</div>
 <div class="mini-stat-card tone-b"><div class="mini-stat-head"><span class="mini-stat-icon">${icon('chart')}</span><span class="kpi-label">صفقات مكتسبة هذا الأسبوع</span></div><strong class="kpi-value" dir="ltr">${fmtNum(currentKpis?.wonDeals?.value)}</strong>${renderSparkline(wonSeries.map(p=>p.value),'chart-c4')}</div>
</div>
<div class="gauge-row">
 <section class="panel"><h3>حالة فريق الوكلاء</h3>${renderDonut(byStatus.map(s=>({label:s.label,value:s.value,colorClass:statusColors[s.status]})),escape)}<ul class="icon-row-list">${byStatus.filter(s=>s.value>0).map(s=>`<li><span class="icon-row-badge ${statusColors[s.status]}">${icon(statusIcons[s.status])}</span><span class="icon-row-copy"><strong>${fmtNum(s.value)} وكيل</strong><small>${escape(s.label)}</small></span></li>`).join('')||''}</ul></section>
 <section class="panel"><h3>معدل التحويل</h3><strong class="kpi-value" dir="ltr">${currentKpis?.conversionRate?.value??'—'}${currentKpis?.conversionRate?.value!=null?'%':''}</strong><p class="kpi-context">${currentKpis?.conversionRate?.value!=null?'من فرصة إلى صفقة مكتسبة، هذا الأسبوع':'لا فرص جديدة هذا الأسبوع لحساب المعدل'}</p>${renderTrendChart(weeklySeries(reportsData,k=>k.conversionRate.value),escape,'chart-c3')}</section>
 <section class="panel"><h3>توزيع الفرص حسب المرحلة</h3>${renderDonut(stageSegments,escape)}</section>
</div>
<div class="overview-grid"><section class="panel overview-brief"><div class="row-between"><h2>${icon('agent')} موجز Frost</h2>${badge(frost?.gate?.paused?'متوقف مؤقتًا':frost?.schedulerRunning?'الجدولة نشطة':'الجدولة غير نشطة',frost?.schedulerRunning&&!frost?.gate?.paused?'ONLINE':'HOLD')}</div><p>لقطة مباشرة من مساحة العمل</p><div class="overview-list"><div><span>محتوى الغد<small>بحسب خطة المحتوى الحالية</small></span><strong>${plan?.brief?.tomorrowContent?.length??'—'}</strong></div><div><span>قرارات الوكلاء المعلقة</span><strong>${approvals.length}</strong></div><div><span>خانات الغد غير المجدولة</span><strong>${plan?.brief?.gaps?.length??'—'}</strong></div></div><p>${plan?.brief?.calendarMissing?'لم تُنشأ خطة محتوى الغد بعد. افتح التقويم لإعدادها.':'راجع القرارات والخانات المتاحة قبل تجهيز المحتوى.'}</p><button type="button" class="secondary" data-go="agents">فتح غرفة التحكم ${icon('arrow')}</button></section><section class="panel"><h2>إجراءات سريعة</h2><p>ابدأ العمل من هنا</p><div class="quick-grid"><button type="button" class="secondary" data-go="content">${icon('file')} استوديو المحتوى</button><button type="button" class="secondary" data-go="planning">${icon('calendar')} التقويم والجدولة</button><button type="button" class="secondary" data-go="knowledge">${icon('book')} ذاكرة العلامة</button><button type="button" class="secondary" data-go="reports">${icon('chart')} التقرير الأسبوعي</button></div></section><section class="panel"><div class="row-between"><h2>يحتاج انتباهك</h2>${badge(pending.length+' محتوى','PENDING')}</div><div class="overview-list">${pending.slice(0,5).map(c=>`<a href="#content"><span>${escape(c.title)}<small>${escape(c.platform)} · ${escape(c.date)}</small></span>${badge(c.status==='DRAFT'?'مراجعة':'اعتماد',c.status)}</a>`).join('')||empty('لا محتوى ينتظر قرارًا','ستظهر المسودات وطلبات الاعتماد هنا.')}</div></section><section class="panel"><h2>آخر النشاطات</h2><div class="overview-list">${state.audit.slice(0,5).map(a=>`<div><span>${escape(a.actorName||'سجل سابق')}<small>${escape(actionLabels[a.action]||a.action)}</small></span><small>${escape(new Date(a.at).toLocaleDateString('ar-SA',{timeZone:'Asia/Riyadh'}))}</small></div>`).join('')||empty('لا نشاط مسجل بعد')}</div></section><section class="panel overview-wide"><div class="row-between"><h2>العملاء والمبيعات</h2><button type="button" class="ghost" data-go="crm" ${user.role==='reviewer'?'hidden':''}>عرض المبيعات ${icon('arrow')}</button></div>${crm?.leads?.length?`<div class="overview-list">${crm.leads.slice(0,5).map(l=>`<div><span>${escape(l.name)}<small>${escape(l.company||l.productNeed||'')}</small></span>${badge(l.stage)}</div>`).join('')}</div>`:empty(user.role==='reviewer'?'بيانات المبيعات مخصصة للمالك والمشغل':'لا توجد فرص مسجلة','تظهر مؤشرات المبيعات بعد إضافة أول عميل.')}</section></div>`;host.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>navigate(b.dataset.go));host.querySelectorAll('a[href^="#"]').forEach(a=>a.onclick=e=>{e.preventDefault();navigate(a.hash.slice(1));});installTrendChart(host);}
function filterContent(){const state=cache.get('/api/state');if(!state)return;const q=$('#content-search').value.toLowerCase(),status=$('#content-status').value,platform=$('#content-platform').value;$('#items').querySelectorAll(':scope>article').forEach(card=>{const item=state.content.find(c=>c.id===card.dataset.itemId);card.hidden=!!item&&(!`${item.title} ${item.body}`.toLowerCase().includes(q)||(status&&item.status!==status)||(platform&&item.platform!==platform));});$('#content-no-results')?.remove();if(state.content.length&&!$('#items').querySelector(':scope>article:not([hidden])')){const div=document.createElement('div');div.id='content-no-results';div.innerHTML=empty('لا توجد نتائج مطابقة','غيّر البحث أو عوامل التصفية.');$('#items').append(div);}}
export function openContentItem(id){const card=[...$('#items').children].find(el=>el.dataset.itemId===id);if(card){card.hidden=false;drawer('مراجعة المحتوى',card,{restore:true});}}
function renderCollections(){const state=cache.get('/api/state');$('#audit-list').innerHTML=state.audit.length?table(['العملية','بواسطة','التاريخ والوقت','التفاصيل'],state.audit.map((a,i)=>[escape(actionLabels[a.action]||a.action),escape(a.actorName||'غير مسجل'),`<time dir="ltr">${escape(new Date(a.at).toLocaleString('en-GB',{timeZone:'Asia/Riyadh'}))}</time>`,`<button type="button" class="ghost" data-audit-index="${i}">عرض التفاصيل</button>`])):empty('لا توجد عمليات مسجلة');$('#audit-list').querySelectorAll('[data-audit-index]').forEach(b=>b.onclick=()=>{const node=document.createElement('pre');node.textContent=JSON.stringify(state.audit[Number(b.dataset.auditIndex)],null,2);drawer('تفاصيل العملية',node,{restore:true});});
 const pending=state.content.filter(c=>['DRAFT','REVIEWED'].includes(c.status));
 const issues=cache.get('/api/content/dashboard')?.complianceIssues;
 const severityLabel={HIGH:'عالٍ',MEDIUM:'متوسط',LOW:'منخفض'};
 const issuesBlock=!issues?'':`<div class="section-title"><h2>ملاحظات الامتثال الآلي</h2><span>${issues.rows.length} ملاحظة</span></div>`+(issues.rows.length?`<div class="risk-list">${issues.rows.map(row=>`<div class="risk-item"><div class="risk-body"><span class="risk-title">${escape(row.title)} · ${escape(row.category)}</span><span class="risk-meta">${escape(severityLabel[row.severity]||row.severity)} · ${escape(row.field)}: ${escape(row.problem)}${row.correction?' — اقتراح: '+escape(row.correction):''}</span></div><button type="button" class="secondary" data-content-open="${escape(row.contentId)}">فتح المحتوى</button></div>`).join('')}</div>`:empty('لا توجد ملاحظات امتثال آلية مفتوحة','الفحص الآلي مساعد فقط ولا يغني عن المراجعة البشرية.'));
 $('#content-approval-view').innerHTML=`<div class="section-title"><h2>قائمة المراجعة والاعتماد</h2><span>${pending.length} عنصر</span></div>`+(pending.length?table(['المحتوى','المنصة','الحالة','الإجراء'],pending.map(c=>[escape(c.title),escape(c.platform),badge(c.status==='DRAFT'?'يحتاج مراجعة':'يحتاج اعتماد',c.status),`<button type="button" class="secondary" data-content-open="${escape(c.id)}">فتح المراجعة</button>`])):empty('لا توجد موافقات معلقة'))+issuesBlock;
 const jobs=cache.get('/api/planning')?.jobs||[];$('#content-publishing-view').innerHTML=`<div class="panel"><h2>قائمة النشر والجدولة</h2><p>الجدولة تحفظ الموعد؛ النشر الفعلي يعتمد على ربط المنصة.</p>${jobs.length?table(['المحتوى','الحالة','موعد الرياض'],jobs.map(j=>[escape(j.snapshot.title),badge(j.status),escape(new Date(j.scheduledAt).toLocaleString('ar-SA',{timeZone:'Asia/Riyadh'}))])):empty('لا توجد مواعيد مجدولة')}<button type="button" class="secondary" id="open-publishing-calendar">إدارة الجدولة</button></div>`;$('#open-publishing-calendar').onclick=()=>navigate('planning');}
let calendarMode='month',calendarDate='',calendarPlatform='';
function renderCalendar(){const plan=cache.get('/api/planning');if(!plan)return;calendarDate ||= plan.slots[0]?.date||plan.today;const host=$('#calendar-view');host.innerHTML='<div class="calendar-controls"></div><div class="calendar-output"></div>';const controls=host.firstChild;for(const [key,label] of [['month','شهر'],['week','أسبوع'],['agenda','أجندة']]){const b=button(label,{variant:calendarMode===key?'primary':'secondary','aria-pressed':String(calendarMode===key)});b.onclick=()=>{calendarMode=key;renderCalendar();enhance(host);};controls.append(b);}const date=document.createElement('input');date.type='date';date.value=calendarDate;date.setAttribute('aria-label','تاريخ التقويم');date.onchange=()=>{if(date.value)calendarDate=date.value;renderCalendar();enhance(host);};const select=document.createElement('select');select.setAttribute('aria-label','منصة التقويم');select.innerHTML='<option value="">كل المنصات</option>'+['Instagram','X','Facebook','LinkedIn'].map(p=>`<option>${p}</option>`).join('');select.value=calendarPlatform;select.onchange=()=>{calendarPlatform=select.value;renderCalendar();enhance(host);};controls.append(date,select);const output=host.lastChild,slots=plan.slots.filter(s=>!calendarPlatform||s.platform===calendarPlatform);if(!slots.length){output.innerHTML=empty('لا توجد خانات في التقويم','أنشئ خطة محتوى أو غيّر عامل تصفية المنصة.');return;}const content=cache.get('/api/state').content;if(calendarMode==='agenda'){output.innerHTML=table(['التاريخ','المنصة','الفكرة','المحتوى'],slots.map(s=>[escape(s.date),escape(s.platform),escape(s.pillar),escape(content.find(c=>c.id===s.contentId)?.title||'بانتظار محتوى معتمد')]));return;}const selected=new Date(calendarDate+'T00:00:00Z'),start=new Date(selected);if(calendarMode==='month')start.setUTCDate(1);start.setUTCDate(start.getUTCDate()-start.getUTCDay());const dayCount=calendarMode==='week'?7:Math.ceil((new Date(selected.getUTCFullYear(),selected.getUTCMonth()+1,0).getDate()+new Date(Date.UTC(selected.getUTCFullYear(),selected.getUTCMonth(),1)).getUTCDay())/7)*7;let html=['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'].map(day=>`<div class="calendar-heading">${day}</div>`).join('');for(let i=0;i<dayCount;i++){const d=new Date(start);d.setUTCDate(start.getUTCDate()+i);const key=d.toISOString().slice(0,10);html+=`<div class="calendar-day"><time datetime="${key}">${d.getUTCDate()} / ${d.getUTCMonth()+1}</time>${slots.filter(s=>s.date===key).map(s=>`<span class="calendar-event">${escape(s.platform)}<br>${escape(content.find(c=>c.id===s.contentId)?.title||s.pillar)}</span>`).join('')}</div>`;}output.innerHTML=`<div class="table-scroll" tabindex="0" role="region" aria-label="التقويم قابل للتمرير"><div class="calendar-grid">${html}</div></div>`;}
